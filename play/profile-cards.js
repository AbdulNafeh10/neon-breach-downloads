export function milestones(stats={}){return [{name:'First victory',value:+stats.wins||0,goal:1},{name:'Arena veteran',value:+stats.matches||0,goal:25},{name:'Century club',value:+stats.kills||0,goal:100},{name:'Unstoppable',value:+stats.best||0,goal:5}];}
export function achievementCards(stats){const root=document.createElement('div');root.className='achievement-grid';for(const m of milestones(stats)){const card=document.createElement('article');card.className=m.value>=m.goal?'achievement unlocked':'achievement';const title=document.createElement('b');title.textContent=m.name;const progress=document.createElement('small');progress.textContent=m.value>=m.goal?'UNLOCKED':Math.min(m.value,m.goal)+' / '+m.goal;card.append(title,progress);root.append(card);}return root;}
